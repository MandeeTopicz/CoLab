## Gemini (Google AI Studio) API key (server-side only)
# Never commit real keys. Copy to `server/.env` locally or set in Cloud Run env vars.
GEMINI_API_KEY=your_api_key_here

## Optional: choose Gemini model
# GEMINI_MODEL=gemini-1.5-flash

